# TokenScribe — Python Dependencies
# Author: Matteo Morreale
# Install with: pip install -r requirements.txt

Flask==3.1.0
Werkzeug==3.1.3
Jinja2==3.1.4

# LLM Provider SDKs
openai>=1.30.0
anthropic>=0.28.0
google-generativeai>=0.8.0
# grpcio-status must stay in sync with grpcio (same minor version) to avoid
# NameError: name 'rpc_status' is not defined in google.api_core.exceptions
grpcio>=1.71.0
grpcio-status>=1.71.0
# mistralai v2 uses: from mistralai.client.sdk import Mistral
mistralai>=2.0.0

# Tokenisation & Embeddings
tiktoken>=0.7.0
sentence-transformers>=3.0.0
numpy>=1.26.0

# Utilities
requests>=2.32.0
python-dotenv>=1.0.0
cryptography>=42.0.0
